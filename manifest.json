{
  "name": "BT Catalog",
  "slug": "bt-catalog",
  "version": "0.24.1",
  "download_url": "https://raw.githubusercontent.com/strummer95/bt-catalog/main/bt-catalog-0.24.1.zip",
  "homepage": "https://boomerts.com",
  "tested": "6.6",
  "requires": "5.8",
  "changelog": "<h4>0.24.1 &mdash; jump the queue for one brand</h4><ul><li>A full SanMar run is ~3,100 styles at 25/min, about two hours. You no longer have to wait it out for one brand.</li><li>New box under Step 2: type a style prefix and it moves every matching style still in the queue to the front, then runs the first 25 straight away. <strong>New Era is NE.</strong> Richardson is R, Sport-Tek is ST.</li><li>Styles already processed stay put, the rest keep their order, and nothing leaves the queue &mdash; the full import still finishes, just in a different sequence.</li></ul><h4>0.24.0 &mdash; stop skipping whole brands</h4><ul><li><strong>New Era, and anything else the brand skip list was hiding, will now import from SanMar.</strong> The list shipped with twelve brands on it &mdash; Gildan, Hanes, New Era and the rest &mdash; and dropped every SanMar style in them.</li><li>That was never the ask. The ask was to skip SanMar styles S&amp;S already has, which the import does per style, by style number. Skipping a whole brand also threw away the styles in it that S&amp;S doesn't carry.</li><li>The list is now empty by default and purely optional &mdash; for refusing a brand from SanMar outright. A saved copy of the old default is cleared for you; a list you edited by hand is left exactly as it is.</li><li><em>To pick up what was being skipped: SanMar &rarr; Discover styles &rarr; Start import.</em> Nothing duplicates.</li></ul><h4>0.23.0</h4><ul><li>Every BT plugin now shows the same <strong>Updates</strong> section in the same place &mdash; last on the page, same heading, same button, same wording. Shared file, so they cannot drift apart again.</li><li>The GitHub repo field is now its own <em>Update source</em> setting above it.</li></ul>Fixes 'Style not found' for brands whose style codes contain no digits. Splitting 'Shaka SHGDSSP' into brand + style required the trailing token to contain a number, on the assumption that style numbers are numeric — every Shaka Wear code is pure letters, so the split never happened, the whole string got searched as one style name, and the lookup failed on styles that are plainly in the catalog. The split now happens on any trailing token, and falls back to searching the untouched string when the brand + style reading finds nothing."
}